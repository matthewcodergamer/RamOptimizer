# RamOptimizer

RamOptimizer is a lightweight Manifest V3 Chrome extension that watches system memory pressure and intelligently unloads safe, inactive tabs while keeping the current browsing session intact.

The project is intentionally small and dependency-free so the optimizer does not become part of the problem it is trying to solve.

## Status

Initial extension build in progress.
